{
  "name": "cardscan-proxy",
  "version": "1.0.0",
  "description": "CardScan API-Proxy – hält den Anthropic Key server-seitig",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev":   "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "cors":    "^2.8.5",
    "express": "^4.19.0"
  }
}
